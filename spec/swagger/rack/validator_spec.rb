require 'spec_helper'

describe Swagger::Rack::Validator do
  it 'has a version number' do
    expect(Swagger::Rack::Validator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
